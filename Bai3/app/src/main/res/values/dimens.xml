<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- ── Spacing ────────────────────────────────────────── -->
    <dimen name="spacing_xxs">2dp</dimen>
    <dimen name="spacing_xs">4dp</dimen>
    <dimen name="spacing_sm">8dp</dimen>
    <dimen name="spacing_md">12dp</dimen>
    <dimen name="spacing_lg">16dp</dimen>
    <dimen name="spacing_xl">24dp</dimen>
    <dimen name="spacing_xxl">32dp</dimen>
    <dimen name="spacing_xxxl">48dp</dimen>

    <!-- ── Corner Radius ──────────────────────────────────── -->
    <dimen name="radius_sm">8dp</dimen>
    <dimen name="radius_md">12dp</dimen>
    <dimen name="radius_lg">16dp</dimen>
    <dimen name="radius_xl">24dp</dimen>
    <dimen name="radius_round">100dp</dimen>

    <!-- ── Button Sizes ───────────────────────────────────── -->
    <dimen name="btn_circle_size">56dp</dimen>
    <dimen name="btn_circle_size_sm">44dp</dimen>
    <dimen name="btn_circle_size_lg">72dp</dimen>
    <dimen name="btn_rect_height">48dp</dimen>
    <dimen name="btn_min_touch">48dp</dimen>
    <dimen name="btn_icon_size">24dp</dimen>
    <dimen name="btn_icon_size_lg">32dp</dimen>

    <!-- ── D-Pad ──────────────────────────────────────────── -->
    <dimen name="dpad_size">220dp</dimen>
    <dimen name="dpad_center_size">64dp</dimen>
    <dimen name="dpad_arrow_size">52dp</dimen>

    <!-- ── Card ───────────────────────────────────────────── -->
    <dimen name="card_elevation">4dp</dimen>
    <dimen name="card_padding">16dp</dimen>

    <!-- ── Text Sizes ─────────────────────────────────────── -->
    <dimen name="text_display">28sp</dimen>
    <dimen name="text_title">20sp</dimen>
    <dimen name="text_subtitle">16sp</dimen>
    <dimen name="text_body">14sp</dimen>
    <dimen name="text_caption">12sp</dimen>
    <dimen name="text_small">10sp</dimen>

    <!-- ── Bottom Navigation ──────────────────────────────── -->
    <dimen name="bottom_nav_height">64dp</dimen>
    <dimen name="bottom_nav_icon_size">24dp</dimen>

    <!-- ── Status Bar ─────────────────────────────────────── -->
    <dimen name="status_dot_size">8dp</dimen>

    <!-- ── Touchpad ───────────────────────────────────────── -->
    <dimen name="touchpad_padding">24dp</dimen>

    <!-- ── Widget ─────────────────────────────────────────── -->
    <dimen name="widget_padding">8dp</dimen>
    <dimen name="widget_btn_size">40dp</dimen>

    <!-- ── App Launcher Grid ──────────────────────────────── -->
    <dimen name="app_btn_width">84dp</dimen>
    <dimen name="app_btn_height">48dp</dimen>
    <dimen name="app_icon_size">24dp</dimen>

    <!-- ── Volume Strip ───────────────────────────────────── -->
    <dimen name="volume_strip_width">56dp</dimen>
    <dimen name="volume_bar_height">120dp</dimen>
</resources>
