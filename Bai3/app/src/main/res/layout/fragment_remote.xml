<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/remote_scroll"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:fillViewport="true"
    android:fitsSystemWindows="true"
    android:scrollbars="none">

    <LinearLayout
        android:id="@+id/remote_root"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingStart="@dimen/spacing_lg"
        android:paddingEnd="@dimen/spacing_lg"
        android:paddingBottom="48dp">

        <!-- ── Connection Status Bar ──────────────────── -->
        <LinearLayout
            android:id="@+id/status_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingTop="@dimen/spacing_lg"
            android:paddingBottom="@dimen/spacing_md">

            <View
                android:id="@+id/status_dot"
                android:layout_width="@dimen/status_dot_size"
                android:layout_height="@dimen/status_dot_size"
                android:background="@drawable/bg_button_circle" />

            <TextView
                android:id="@+id/tv_name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/spacing_sm"
                android:layout_weight="1"
                android:text="@string/remote_not_connected"
                android:textAppearance="@style/TextAppearance.LGRemotePro.Subtitle"
                android:textColor="@color/text_primary" />

            <TextView
                android:id="@+id/status_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/remote_disconnected"
                android:textAppearance="@style/TextAppearance.LGRemotePro.Caption"
                android:textColor="@color/text_secondary" />
        </LinearLayout>

        <!-- ── Remote Card Content ────────────────────────────── -->            <!-- ── Row 1: Power, Input, Home, Back, Settings ── -->
            <LinearLayout
                android:id="@+id/row_system"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="horizontal">

                <Space android:layout_width="0dp" android:layout_height="0dp" android:layout_weight="0.5"/>

                <ImageButton
                    android:id="@+id/btn_power"
                    style="@style/Widget.LGRemotePro.Button.Circle"
                    android:contentDescription="@string/btn_power"
                    android:src="@drawable/ic_power" />

                <Space android:layout_width="0dp" android:layout_height="0dp" android:layout_weight="1"/>

                <ImageButton
                    android:id="@+id/btn_home"
                    style="@style/Widget.LGRemotePro.Button.Circle"
                    android:contentDescription="@string/btn_home"
                    android:src="@drawable/ic_home" />

                <Space android:layout_width="0dp" android:layout_height="0dp" android:layout_weight="1"/>

                <ImageButton
                    android:id="@+id/btn_back"
                    style="@style/Widget.LGRemotePro.Button.Circle"
                    android:contentDescription="@string/btn_back"
                    android:src="@drawable/ic_back" />

                <Space android:layout_width="0dp" android:layout_height="0dp" android:layout_weight="1"/>

                <ImageButton
                    android:id="@+id/btn_settings_tv"
                    style="@style/Widget.LGRemotePro.Button.Circle"
                    android:contentDescription="@string/btn_settings"
                    android:src="@drawable/ic_settings_gear" />

                <Space android:layout_width="0dp" android:layout_height="0dp" android:layout_weight="0.5"/>
            </LinearLayout>

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginTop="@dimen/spacing_lg"
                android:layout_marginBottom="@dimen/spacing_lg"
                android:background="@color/glass_border" />

            <!-- ── D-Pad Section ──────────────────────── -->
            <include
                android:id="@+id/dpad_layout"
                layout="@layout/layout_dpad"
                android:layout_width="150dp"
                android:layout_height="150dp" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginTop="@dimen/spacing_lg"
                android:layout_marginBottom="@dimen/spacing_lg"
                android:background="@color/glass_border" />

            <!-- ── Volume & Channel Strips ────────────── -->
            <include
                android:id="@+id/volume_strip_layout"
                layout="@layout/layout_volume_strip"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginTop="@dimen/spacing_lg"
                android:layout_marginBottom="@dimen/spacing_lg"
                android:background="@color/glass_border" />

            <!-- ── Media Controls ─────────────────────── -->
            <include
                android:id="@+id/media_controls_layout"
                layout="@layout/layout_media_controls"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginTop="@dimen/spacing_lg"
                android:layout_marginBottom="@dimen/spacing_lg"
                android:background="@color/glass_border" />

            <!-- ── Quick App Launcher ─────────────────── -->
            <include
                android:id="@+id/app_launcher_layout"
                layout="@layout/layout_app_launcher"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

            <!-- ── Number Pad (Collapsible) ───────────── -->
            <include
                android:id="@+id/number_pad_layout"
                layout="@layout/layout_number_pad"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:visibility="gone" />


        <!-- ── Number Pad Toggle ──────────────────────── -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_toggle_numpad"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_sm"
            android:text="@string/btn_show_numpad"
            android:textColor="@color/primary_accent" />

    </LinearLayout>

</ScrollView>
